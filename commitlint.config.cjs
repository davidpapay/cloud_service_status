// Commitlint configuration file
// Enforces the Conventional Commits standard in this repository.
// This ensures consistent and professional commit messages such as:
// feat: add new feature
// docs: update documentation
// ci: update GitHub Actions pipeline

module.exports = {
  extends: ['@commitlint/config-conventional'],
};
